<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Camera permission + required feature tag -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Photos access - handles Android 14+ Selected Photos Access -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Internet for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_assignment">

        <!-- Activity 1 - Launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity 2 - ML Kit -->
        <activity
            android:name=".MLKitActivity"
            android:exported="false" />

        <!-- Activity 5 - Edit -->
        <activity
            android:name=".EditActivity"
            android:exported="false" />

        <!-- Activity 6 - List View -->
        <activity
            android:name=".ListViewActivity"
            android:exported="false" />

        <!-- Activity 7 - Item Detail -->
        <activity
            android:name=".ItemDetailActivity"
            android:exported="false" />

    </application>

</manifest>